-- 트랜잭션

-- DEPT테이블 복사
CEATE TABLE DEPT_TCL
   AS SELECT *
        FROM DEPT;

SELECT * FROM DEPT_TCL;
SELECT * FROM EMP_TEMP;

-- 트랜잭션 시작
COMMIT;

INSERT INTO DEPT_TCL (DEPT, DNAME, LOC)
VALUES (60, 'UNIT TEST', 'DELI');

ROLLBACK;

-- 트랜잭션 시작2
INSERT INTO DEPT_TCL (DEPT, DNAME, LOC)
VALUES (60, 'UNIT TEST', 'DELI');

UPDATE DEPT_TCL SET LOC = 'BUSAN'
 WHERE DEPTNO = 40;

DELETE FROM DEPT_TCL WHERE DNAME = 'RESERCH';

DELETE FROM EMP_TEMP WHERE EMPNO = 9999;

SELECT * FROM DEPT_TCL;
SELECT * FROM EMP_TEMP;

COMMIT;
ROLLBACK;

DELETE FROM EMP_TEMP;

SELECT * FROM EMP_TEMP;

-- 세션, 락
SELECT * FROM DEPT_TCL;

UPDATE DEPT_TCL
   SET LOC='INCHOEN'
 WHERE DEPT = 30;

COMMIT;

-- 락2
DELETE FROM DEPT_TCL
 WHERE DEPTNO = 60;

ROLLBACK;
